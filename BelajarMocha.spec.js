import { expect } from "chai"

describe('fitur penjumlahan', function () {
    it('Test 1 + 1 hasilnya 2', function() {
        //asertion
        const penjumlahan = 1 + 1
        expect(penjumlahan).to.equal(2)
    })
})